label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") #+ coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) - (0.01 * value))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") #+ coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) - (0.001 * value))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") #+ coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) - (0.0001 * value))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") #+ coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) - (0.000001 * value))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") #+ coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) - (0.002 * value))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") #+ coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) - ( value))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") #+ coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) + ( 0.01 * value))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") #+ coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) + (value))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") #+ coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) + (0.001 * value))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") #+ coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) + (0.0001 * value))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) + (0.0001 * value))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) + (value/2))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) - (value/2))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) - (value/4))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) + (value/4))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) - (value/10))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) - (value * 0.10))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) #- (value * 0.10))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = cumsum(value) )#- (value * 0.10))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
df <- ddply(df, .(Year_Quarter),
transform, pos = value) #cumsum(value) )#- (value * 0.10))
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = pos,
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = value - (value*.01),
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = value,# - (value*.01),
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = value + (value*.01),
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = value + (value*.1),
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
stacked <- stacked + geom_text(data=df, aes(x = Year_Quarter,
y = value + (value*.5),
label = value),
size = 4)
stacked
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + ggtitle(
"RTR by year, quarter") +
labs(x="Years, quarters",
y="Types of response-to-resistance")
stacked
?ggtitle
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + labs(
title="RTR by year, quarter",
subtitle="subtitle here"
x="Years, quarters",
y="Types of response-to-resistance")
stacked
?ggtitle
stacked <- ggplot() + geom_bar(data=df,
aes(Year_Quarter,
value,
fill = force),
position = "stack",
stat="identity") + coord_flip()
stacked <- stacked + theme(legend.position="top", legend.direction="horizontal",
legend.title = element_blank())
stacked <- stacked + labs(
title="RTR by year, quarter",
subtitle="subtitle here",
x="Years, quarters",
y="Types of response-to-resistance",
caption="caption here")
stacked
